
module Pair where

import Prelude hiding ((*), not)
import Tree

pair :: Tree
pair = Node

first :: Tree -> Tree
first p = Node * p * Node * k

second :: Tree -> Tree
second p = Node * p * Node * ki
